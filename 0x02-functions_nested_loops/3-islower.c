#include "main.h"
/**
*print_alphabet_x10 - checks for lowercase character
* 
*desc: The function uses loop to operate
*Return: no return
*/

int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);

return (0);	
}
